var id = <%== @order.id%>
if (<%== @success%>) {
console.log('success')
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$('#orderShowModal').modal('hide');
$('.admin-info').html("<%= escape_javascript(render 'admin/orders/partial/index') %>")
toastr.success("登録情報を変更いたしました。")
} else {
$('.admin-order-new-content').html("<%= escape_javascript(render 'orders/edit_form', remote: 'true', url: `/admin/orders/${id}` ) %>")
}

