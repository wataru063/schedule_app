<%= render 'shared/error_messages', object: f.object %>

<%= f.label :name, 'ユーザー名', class: "#{form_class}", id: "u-name-label" %>
<%= f.text_field :name, class: 'form-control', id: "u-name" %>

<%= f.label :email, 'メールアドレス', class: "#{form_class}", id: "u-email-label" %>
<%= f.email_field :email, class: 'form-control', id: "u-email" %>

<div class=" form-group">
  <%= f.label :category_id, 'カテゴリー', class: "#{form_class}" %>
  <%= f.collection_select :category_id, Category.all, :id, :name, {}, class: 'form-control' %>
</div>